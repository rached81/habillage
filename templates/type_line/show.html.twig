{% extends 'base.html.twig' %}

{% block title %}TypeLine{% endblock %}

{% block body %}
    <h1>TypeLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_line.id }}</td>
            </tr>
            <tr>
                <th>Label_ar</th>
                <td>{{ type_line.labelAr }}</td>
            </tr>
            <tr>
                <th>Label_fr</th>
                <td>{{ type_line.labelFr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_line_index') }}">back to list</a>

    <a href="{{ path('type_line_edit', {'id': type_line.id}) }}">edit</a>

    {{ include('type_line/_delete_form.html.twig') }}
{% endblock %}
