{% extends 'base.html.twig' %}

{% block title %}Networks{% endblock %}

{% block body %}
    <h1>Networks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ network.id }}</td>
            </tr>
            <tr>
                <th>Label_fr</th>
                <td>{{ network.labelFr }}</td>
            </tr>
            <tr>
                <th>Label_ar</th>
                <td>{{ network  .labelAr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('networks_index') }}">back to list</a>

    <a href="{{ path('networks_edit', {'id': network.id}) }}">edit</a>

    {{ include('networks/_delete_form.html.twig') }}
{% endblock %}
