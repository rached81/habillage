{% extends 'base.html.twig' %}

{% block title %}ConstraintType{% endblock %}

{% block body %}
    <h1>ConstraintType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ constraint_type.id }}</td>
            </tr>
            <tr>
                <th>Label_fr</th>
                <td>{{ constraint_type.labelFr }}</td>
            </tr>
            <tr>
                <th>Label_ar</th>
                <td>{{ constraint_type.labelAr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('constraint_type_index') }}">back to list</a>

    <a href="{{ path('constraint_type_edit', {'id': constraint_type.id}) }}">edit</a>

    {{ include('constraint_type/_delete_form.html.twig') }}
{% endblock %}
